# Define desired order
month_order <- month.abb  # gives: "Jan", "Feb", ..., "Dec"

# Reorder columns
monthly_totals <- monthly_totals[, c("year", month_order[month_order %in% names(monthly_totals)])]

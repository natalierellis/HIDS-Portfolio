🧪 Why "1 surgery, 2 path reports"?
Because:

One surgery may remove tissue from multiple sites (e.g., uterus and colon).

If two distinct cancers are present, there should be:

Two distinct pathology results confirming two primaries.

Possibly two different sites in site_p, site_p2, etc., from different anatomic origins.

So, she's likely pointing out that to verify true concurrency, the patient record should:

Have one surgery that captures multiple specimens.

Show two distinct pathology reports (or fields

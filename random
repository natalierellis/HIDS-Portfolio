# Reshape and clean
df_long <- SigHRSubgroupAnalysis %>%
  pivot_longer(cols = -1,
               names_to = "TimeInterval",
               values_to = "HR") %>%
  rename(Subgroup = 1) %>%
  filter(!is.na(HR)) %>%
  mutate(TimeInterval = factor(TimeInterval, levels = ordered_levels))

# Plot using color aesthetic mapped to PointColor
ggplot(df_long, aes(x = TimeInterval, y = HR)) +
  geom_point(position = size = 2.5) +
  theme_minimal() +
  labs(title = "Hazard Ratios by Time Interval and Subgroup",
       x = "Time Interval", y = "Hazard Ratio",
       color = "Subgroup") +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))

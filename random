library(gtsummary)

NCDB %>%
  select(TTC) %>%
  tbl_summary(
    statistic = list(
      all_continuous() ~ "{mean} ({sd})\nMedian = {median}\nQ1 = {p25}, Q3 = {p75}\nMin = {min}, Max = {max}"
    ),
    missing = "no"  # or "ifany"
  ) %>%
  as_gt()
